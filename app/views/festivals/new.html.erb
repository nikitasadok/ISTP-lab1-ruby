<h1>New Festival</h1>

<%= form_with(model: @festival, local: true) do |form| %>
 
  <% if @festival.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@festival.errors.count, "error") %> prohibited
        this festival from being saved:
      </h2>
      <ul>
        <% @festival.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
    <%= form.label :name %><br>
    <%= form.text_field :name %>
  </p>

   <p>
    <%= form.label :start_date %><br>
    <%= form.text_field :start_date%>
  </p>

   <p>
    <%= form.label :end_date %><br>
    <%= form.text_field :end_date %>
  </p>

  <p>
  <%= form.label :organizer_id %><br>
  <%= form.collection_select :organizer_id, Organizer.all, :id, :name %>
  </p>



  <p>
    <%= form.submit %>
  </p>
 
<% end %>

<%=link_to 'Back', festivals_path %>