<h1> New performer style </h1>

<%= form_with(model: @performers_style, local: true) do |form| %>
 
  <% if @performers_style.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@performers_style.errors.count, "error") %> prohibited
        this performer style from being saved:
      </h2>
      <ul>
        <% @performers_style.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
    <%= form.label :performer_id %><br>
    <%= form.collection_select :performer_id, Performer.all, :id, :name %>
  </p>

  <p>
    <%= form.label :style_id %><br>
    <%= form.collection_select :style_id, Style.all, :id, :name %>
  </p>

  <p>
    <%= form.submit %>
  </p>
 
<% end %>

<%=link_to 'Back', welcome_index_path %>